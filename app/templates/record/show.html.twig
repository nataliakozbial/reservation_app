{% extends 'base.html.twig' %}

{% block title %}
    Podgląd
{% endblock %}

{% block body %}
    <h1 align="center">Szczegóły</h1>
    {% if item is defined and item|length %}
        <dl>
            <dt>Tytuł</dt>
            <dd>{{ item.Tytuł }}</dd>
            <dt>Opis</dt>
            <dd>{{ item.content }}</dd>
            <dt>Tagi</dt>
            <dd>{{ item.tagi|join(', ') }}</dd>
        </dl>
    {% else %}
        <p>
            Record not found.
        </p>
    {% endif %}
    <p>
        <a href="{{ url('record_index') }}" title="Powrót">
            Powrót
        </a>
    </p>
{% endblock %}