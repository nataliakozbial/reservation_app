{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_book_list'|trans }}
{% endblock %}

{% block body %}

    <div class="topnav">
    <a href="{{ url('book_create') }}" title="{{ 'book_new'|trans }}">
        {{ 'book_new'|trans }}
    </a>
    <a href="{{ url('category_index') }}" title="{{ 'category_list'|trans }}">
        {{ 'category_list'|trans }}
    </a>
        <a href="{{ url('app_login') }}" title="{{ 'login'|trans }}">
            {{ 'login'|trans }}
        </a>
        <a href="{{ url('registration') }}" title="{{ 'register'|trans }}">
            {{ 'register'|trans }}
        </a>
    </div>
    {% if pagination is defined and pagination.items|length %}


        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'label_id'|trans, 'book.id') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label_created_at'|trans, 'book.createdAt') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label_updated_at'|trans, 'book.updatedAt') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label_title'|trans, 'book.title') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label_category'|trans, 'book.actions') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label_actions'|trans, 'book.actions') }}</th>
            </tr>
            </thead>
            <tbody>
            {% for book in pagination.items %}
                <tr>
                    <td>{{ book.id }}</td>
                    <td>{{ book.createdAt|format_date('long') }} </td>
                    <td>{{ book.updatedAt|format_date('long') }} </td>
                    <td>{{ book.title }}</td>
                    <td>{{ book.category.title }}</td>
                    <td>
                        <a href="{{ url('book_show', {id: book.id}) }}" title="{{ 'action_view'|trans }}">
                            {{ 'action_view'|trans }}
                        </a>
                        <a href="{{ url('book_edit', {id: book.id}) }}" title="{{ 'action_edit'|trans }}">
                            {{ 'action_edit'|trans }}
                        </a>
                        <a href="{{ url('book_delete', {id: book.id}) }}" title="{{ 'action_delete'|trans }}">
                            {{ 'action_delete'|trans }}
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>

    {% else %}
        <p>
            {{ 'message_empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}